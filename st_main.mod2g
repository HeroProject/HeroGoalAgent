use dialogflow as knowledge.
use dialogflow as actionspec.

module st_main {
	
	% Saying things
	if bel(currentState(S), state(S, say, _), text(S, T, _))
		then say(T).
		
	% Asking things
	if bel(currentState(S), state(S, question, _), text(S, T, _))	
		then say(T).
}