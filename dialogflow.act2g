use dialogflow as knowledge.

% ROBOT ACTIONS
define say(Text) with
	pre { start }
	post { not(start), waitingForEvent('TextDone') }
	
define startListening with
	pre { true }
	post { true }
	
define startListening(Context) with
	pre { true }
	post { true }
	
define stopListening with
	pre { true }
	post { true }
	
define gesture(Gesture) with
	pre { true }
	post { true }

define setEyeColour(Colour)  with
	pre { true }
	post { true }
	
define takePicture with
	pre { true }
	post { true }
	
define setIdle with
	pre { true }
	post { true }
	
define setNonIdle with
	pre { true }
	post { true }
	
define startWatching with
	pre { true }
	post { true }
	
define stopWatching with
	pre { true }
	post { true }
	
% TABLET ACTIONS
define showImage(Url) with
	pre { true }
	post { true }

define showCaptionedImage(Url,Text) with
	pre { true }
	post { true }
	
define showVideo(Url) with
	pre { true }
	post { true }
	
define showWebpage(Url) with
	pre { true }
	post { true }
	
define showText(Text) with
	pre { true }
	post { true }
	
define askYesNo(Question) with
	pre { true }
	post { true }
	
define askRating(Question) with
	pre { true }
	post { true }
	
define askConfirmation(Name) with
	pre { true }
	post { true }

% DIALOGFLOW CONTROL
define setAgent(Agent) with
	pre { true }
	post { true }
	
define setLanguage(Lang) with
	pre { true }
	post { true }