use dialogflow as knowledge.
use dialogflow as actionspec.

module st_init {
	if true then
		insert(currentState(s1), start, flow(1), mood(h), lang('nl-NL')) + 
		setEyeColour('white') + setIdle + startWatching.
}