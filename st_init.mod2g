use dialogflow as knowledge.
use dialogflow as actionspec.

module st_init {
	if true then
		insert(currentState(s1), start, currentAttempt(0)) + setEyeColour('white'). %To do: create initialization state for all robot related initialization.
}