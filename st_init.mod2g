use dialogflow as knowledge.
use dialogflow as actionspec.

module st_init {
	% Initialize state parameters.
	if true then
		insert(currentState(s1), currentAttempt(1), start, nextCondition('true'), answers([])).
}